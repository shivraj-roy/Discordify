import { Center, Box, Title } from "@mantine/core";

const Header = () => {
   return (
      <Center mt={20}>
         <Title order={1}>Discordify</Title>
      </Center>
   );
};
export default Header;
